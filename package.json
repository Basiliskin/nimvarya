{
  "name": "nimvarya",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone, Chrome-control bridge for MCP-capable terminal AI coding tools",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run",
    "relay": "tsx src/relay/main.ts",
    "build:extension": "vite build -c vite.extension.config.ts --mode service-worker && vite build -c vite.extension.config.ts --mode page-script && vite build -c vite.extension.config.ts --mode capture-forwarder",
    "verify": "npm run typecheck && npm run lint && npm run test",
    "mcp": "tsx src/mcp/main.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "eslint": "^9.17.0",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.66.0",
    "vite": "^6.0.7",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ws": "^8.21.3"
  }
}