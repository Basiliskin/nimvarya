# Decisions — agent-agnostic-browser-bridge (binding; never re-litigated)

- 2026-09-02 | horizon 1 | Extraction is copy-and-generalize into a new self-contained package `tools/chrome-bridge/` (own npm package.json, tsconfig, eslint, vitest, `npm run verify`; NOT wired into root — see horizon-01 roadmap.md
- 2026-09-02 | horizon 1 | boky's own extension, BridgeRelayModule and extension-mcp.mjs stay untouched; boky is never migrated onto the new tool in this project — because the objective is reuse, not a boky refactor
- 2026-09-02 | horizon 1 | The relay is re-implemented standalone on `ws` (no Nest), binds 127.0.0.1 only, default port 8766 via CHROME_BRIDGE_PORT — because boky's relay binds 0.0.0.0 on 8765 and cannot boot without Redis/Mongo
- 2026-09-02 | horizon 1 | Wire protocol keeps only hello / command / command-response / observation; command ids are strings (UUID) — because dashboard-* kinds are boky-only and integer ids collide across controllers
- 2026-09-02 | horizon 1 | `PAGE_ACTIONS` (protocol/actions.ts) is the single source of truth; extension handlers and the MCP tool catalog are typed `Record<PageAction, …>` so drift is a tsc error — replaces boky's regex parity script
- 2026-09-02 | horizon 1 | The MCP server exposes one discrete tool per page action (no single ext_command enum tool); captureTab returns an MCP image content block — because Codex/Gemini/Cursor handle enums inconsistently
- 2026-09-02 | horizon 1 | The new extension is built with plain Vite and a hand-written manifest (no @crxjs) — because @crxjs cannot declare a synthesized MAIN-world content script without boky's closeBundle patch
- 2026-09-02 | horizon 2 | Capture delivery is pull-only: entries sit in extension-side per-tab ring buffers and are returned only by readConsoleMessages/readNetworkRequests; no observation-frame streaming, no — see horizon-02 roadmap.md
- 2026-09-02 | horizon 2 | `since` is a monotonic per-buffer sequence number; reads are non-destructive; results carry `nextSince`, `dropped`, `truncated`; a stale since (> newest seq) reads the full window — — see horizon-02 roadmap.md
- 2026-09-02 | horizon 2 | Ring buffers are keyed by tabId, evicted on tabs.onRemoved, and the read tools target the active tab like every other page action; buffers are in-memory only (lost on SW suspension, accepted)
- 2026-09-02 | horizon 2 | Network capture is fetch/XHR only, text-like bodies only, stream-read to MAX_BODY_PREVIEW_BYTES (never clone().text()); nullable fields are `T | null`, never undefined — because — see horizon-02 roadmap.md
- 2026-09-02 | horizon 2 | Content scripts are declared directly in the hand-written manifest (world: MAIN for the page script, ISOLATED forwarder, document_start); built as separate IIFE Vite passes — boky's — see horizon-02 roadmap.md
- 2026-09-02 | horizon 2 | A real Chrome end-to-end check is required to close a horizon that changes the extension; the final phase ends `blocked` (MANUAL_CHROME_CHECK_PENDING) rather than done-but-unverified — — see horizon-02 roadmap.md
- 2026-09-02 | horizon 2 | The wire protocol's `observation` frame stays defined but unused (it has a guard and test); it is not removed — pull-only makes it dead surface, removing it is not worth a phase
- 2026-09-02 | horizon 2 | chrome-bridge es/lint prefer-as-const | Literal-typed constants in tools/chrome-bridge MUST use `as const` — the package eslint rule prefer-as-const errors on an explicit literal type — see horizon-02 roadmap.md
- 2026-09-02 | horizon 3 | executeScript page action runs in the page MAIN world (world: 'MAIN'), not the isolated world, so a caller's expression sees page globals/libraries — because the isolated world cannot evaluate page-scope code
- 2026-09-02 | horizon 3 | clickAt/hover define success as dispatching the pointer/mouse events on the element under the coordinate, never as a framework handler firing — because synthetic events are untrusted and some SPAs ignore them
- 2026-09-03 | horizon 4 | Hand-verification and result-size measurement are done through Claude Code ONLY; Gemini CLI, Codex CLI and Cursor are documentation-only for the life of this project — because Gemini — see horizon-04 roadmap.md
- 2026-09-03 | horizon 6 | CDP (chrome.debugger) is used ONLY to close the background-tab captureVisibleTab gap via Page.captureScreenshot (option a); the broad CDP pivot (Runtime.evaluate, Input.dispatch*, — see horizon-06 roadmap.md
- 2026-09-03 | horizon 6 | The sandbox tab is the ONLY target of every page action (queryActiveTab resolution replaced); the active-tab path is effectively dead but deliberately left in place — because the user — see horizon-06 roadmap.md
- 2026-09-03 | horizon 6 | The 'debugger' manifest permission (a high-privilege permanent grant) is accepted as necessary and deliberate for background-tab screenshots — because the user chose "must capture — see horizon-06 roadmap.md
- 2026-09-03 | horizon 6 | Real-Chrome CDP gate folded into the horizon-closure end check (phase 5 MANUAL_CHROME_CHECK_PENDING) via the chrome-bridge MCP in the real signed-in Chrome 152 after phases 2-5 land — — see horizon-06 roadmap.md
- 2026-09-03 | horizon 7 | getTabState is a READ-ONLY identity report: read the persisted sandbox id via a non-creating peek (never resolveTabId/create), report activeTab from the sandbox tab's OWN windowId — see horizon-07 roadmap.md
- 2026-09-03 | horizon 8 | evalIsolated is a NEW action running in the isolated content-script world (CSP-exempt); executeScript stays MAIN unchanged — because the user chose add-evalIsolated-tool over switching — see horizon-08 roadmap.md
- 2026-09-03 | horizon 8 | Caller-supplied string eval is impossible in an MV3 extension in ANY world (extension_pages CSP forbids 'unsafe-eval', isolated world included) — because measured false live on — see horizon-08 roadmap.md
- 2026-09-03 | horizon 9 | The CDP Runtime.evaluate ban (h6 2026-09-03, h8) is LIFTED for read-only page evaluation, and the broader CDP pivot (Input.dispatch* trusted input, CDP scroll, persistent CDP sessions) — see horizon-09 roadmap.md
- 2026-09-03 | horizon 9 | The evalIsolated action and its full protocol/handler/catalog/test/README surface is DELETED, not frozen-dead like the active-tab surface — because it never functioned and a — see horizon-09 roadmap.md
- 2026-09-03 | horizon 9 | Strict-CSP page value reads go through a new evaluatePage action calling chrome.debugger Runtime.evaluate (returnByValue+awaitPromise) on the sandbox tab; executeScript (MAIN world, — see horizon-09 roadmap.md
- 2026-09-03 | horizon 10 | CDP scroll ships as a new scrollPage action on the sandbox tab via DebuggerPorts.scroll reusing per-command withDebuggerSession attach/detach (NO persistent CDP session); param model — see horizon-10 roadmap.md
- 2026-09-03 | horizon 10 | scrollPage success-fidelity is mechanism-that-moved-the-page: result reports method 'wheel'|'script'|'none' plus scrollY before/after and best-effort reachedEnd — not bare — see horizon-10 roadmap.md
- 2026-09-03 | horizon 10 (replan) | horizon 10 live-verification is narrowed to "scrollPage increases the sandbox tab's window scrollY"; proving virtualized-row materialization is NOT an h10 gate — because the — see horizon-10 roadmap.md
- 2026-09-03 | horizon 10 | scrollPage's steady-state mechanism in the unfocused sandbox tab is "script" (Runtime.evaluate window.scrollBy/scrollTo); the Input.dispatchMouseEvent wheel path hangs there and is — see horizon-10 roadmap.md
- 2026-09-03 | horizon 11 | waitFor ships as a standalone 20th page action (modes selector-present|network-idle|fixed-delay, each bounded by timeoutMs; a timeout yields met:false, not an error), not a {waitFor} — see horizon-11 roadmap.md
- 2026-09-03 | horizon 11 | waitFor selector poll uses chrome.scripting.executeScript (ISOLATED, function ref, no 15s CDP cap, CSP-safe); network-idle derives from the existing per-tab network ring buffer, not a — see horizon-11 roadmap.md
- 2026-09-04 | horizon 12 | A page action's result shape may change IN PLACE (name reused, PAGE_ACTIONS count unchanged) not via a new action; findElement {found,matches} is superseded by — see horizon-12 roadmap.md
- 2026-09-04 | horizon 12 | findElement's per-match element ref is a plain self-contained CSS-locator string re-feedable to findElement/clickElement — never a live handle, CDP node id or XPath; so clickElement — see horizon-12 roadmap.md
- 2026-09-04 | horizon 12 | findElement's result size is bounded IN-PAGE before the structured-clone boundary (match-count cap + per-element innerText cap + truncated/total fields), not handler-side like — see horizon-12 roadmap.md
- 2026-09-04 | horizon 13 | Full-page and single-element screenshots ship by EXTENDING captureTab IN PLACE (PAGE_ACTIONS stays 20): CaptureTabParams gains one mutually-exclusive 3-value `mode` field (viewport — see horizon-13 roadmap.md
- 2026-09-04 | horizon 13 | The screenshot element target is addressed by the h12 findElement CSS-locator ref resolved to a clip rect via CDP Runtime.evaluate document.querySelector(ref).getBoundingClientRect() — see horizon-13 roadmap.md
- 2026-09-04 | horizon 13 | Full-page capture uses CDP Page.captureScreenshot captureBeyondViewport:true and element capture uses clip:{x,y,width,height,scale}, reusing per-command withDebuggerSession (15s cap, — see horizon-13 roadmap.md
- 2026-09-04 | horizon 13 | The screenshot result-size guard is FLAT: an oversized base64 payload returns a defined non-throwing too-large outcome carrying measured bytes + the ceiling constant; downscale / — see horizon-13 roadmap.md
- 2026-09-04 | horizon 13 | mcp/server.ts's toolName==='captureTab' branch is generalised: a real capture still renders as an MCP image block, but a structured too-large / no-match / zero-area outcome is — see horizon-13 roadmap.md
- 2026-09-04 | horizon 13 replan | captureBeyondViewport is infeasible on the unfocused sandbox tab (hangs past the 15s cap / CDP -32000, never returns an image); h13 rev1 ships viewport + element-clip only — see horizon-13 roadmap.md
- 2026-09-04 | horizon 13 replan | the captureTab flat too-large ceiling must sit above the real unfocused-tab viewport base64 (~845KB / 844960 chars at dpr 2, measured live), set to 1_200_000 — because a lower — see horizon-13 roadmap.md
- 2026-09-04 | horizon 13 replan | single-element screenshot targets an element scrolled into view first via scrollPage; un-scrolled below-the-fold clip capture is deferred behind the render-the-tab prerequisite — see horizon-13 roadmap.md
- 2026-09-04 | horizon 13 replan | mcp/server.ts surfaces captureTab width/height/clipped as a text content block alongside the image block on a successful capture — because an image-only response hides the — see horizon-13 roadmap.md
- 2026-09-04 | horizon 14 | The captureTab downscale ladder + size policy live in the page-actions handler layer (page-actions.ts), NOT the debugger port or mcp/server.ts — because the handler owns port+tabId and captureImageOutcome ...
- 2026-09-04 | horizon 14 | Floor-exhaustion REUSES the existing captureTab reason 'too-large' extended with floor+attempts fields (CaptureTabImageResult gains appliedScale+attempts) — a distinct/new CaptureTabFailureReason is deferred ...
- 2026-09-04 | horizon 14 | The h14 downscale ladder is scale-only PNG (rungs e.g. 0.75/0.5/0.33 + a give-up floor); JPEG/quality rungs are deferred because a JPEG rung forces a SOF0 dimension parser, generalized data-URL prefix strip, ...
- 2026-09-04 | horizon 14 | Downscaled viewport capture is done via CDP Page.captureScreenshot with a full-viewport clip at scale<1 built from a Runtime.evaluate layout-viewport geometry read — NOT Emulation.setDeviceMetricsOverride ...
- 2026-09-04 | horizon 14 | The captureTab size guard SUPERSEDES the horizon-13 FLAT no-downscale decision: over-ceiling captures now re-capture smaller down a scale-only ladder — because a flat guard returns no image at all
- 2026-09-04 | horizon 16 | evaluatePage moves in lockstep with executeScript for the non-throwing oversize-result change, since both share the same size constant and throw mechanism today — see horizon-16 roadmap.md
- 2026-09-04 | horizon 16 | closeSandboxTab is an explicit, MCP-caller-invoked action, never automatic on relay-disconnect/SW-suspension — disconnects are routine/auto-reconnecting; explicit-over-implicit is — see horizon-16 roadmap.md
- 2026-09-04 | horizon 16 | The shared oversize-result type is named SizeCeilingOutcome (not OversizeOutcome) to avoid implying captureTab's ladder/downscale semantics — flat ceiling comparison only — see horizon-16 roadmap.md
- 2026-09-04 | horizon 16 | Byte-size caps for readConsoleMessages/readNetworkRequests are explicitly deferred out of this horizon at user request during Alignment Preview — see horizon-16 roadmap.md
- 2026-09-04 | horizon 17 | Oversize readConsoleMessages/readNetworkRequests reads fully replace the result with buildOversizeResult (mirroring executeScript/evaluatePage), no entry-trimming — see horizon-17 roadmap.md
- 2026-09-04 | horizon 17 | Live-measured on wikipedia (limit=500): console 500 entries = 76,671 chars; network 500 entries = 2,331,618 chars, under per-entry caps 8192/4096 — see horizon-17 roadmap.md
